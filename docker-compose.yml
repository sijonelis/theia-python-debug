
version: '3.1'
services:
  theia:
    container_name: theia-python
    build: 
      context:
        ./docker
      dockerfile: dockerfile-dev
    ports: 
      - 3000:3000
    volumes:
      - ./src:/home/theia
